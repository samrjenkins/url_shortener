<%= render "ui/slate" do %>
  <%= render(@shortened_urls) || render('empty_state') %>

  <div class="flex justify-center">
    <%= render "ui/link", path: new_shortened_url_path, content: "Add a new URL" %>
  </div>
<% end %>
